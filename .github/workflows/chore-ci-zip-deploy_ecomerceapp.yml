# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions
# More info on Python, GitHub Actions, and Azure App Service: https://aka.ms/python-webapps-actions

name: Build and deploy Python app to Azure Web App - EcomerceApp

on:
  push:
    branches:
      - chore/ci-zip-deploy
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read #This is required for actions/checkout

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python version
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # 🛠️ Local Build Section (Optional)
      # The following section in your workflow is designed to catch build issues early on the client side, before deployment. This can be helpful for debugging and validation. However, if this step significantly increases deployment time and early detection is not critical for your workflow, you may remove this section to streamline the deployment process.
      - name: Create and Start virtual environment and Install dependencies
        run: |
          python -m venv antenv
          source antenv/bin/activate
          pip install -r requirements.txt
                
      # By default, when you enable GitHub CI/CD integration through the Azure portal, the platform automatically sets the SCM_DO_BUILD_DURING_DEPLOYMENT application setting to true. This triggers the use of Oryx, a build engine that handles application compilation and dependency installation (e.g., pip install) directly on the platform during deployment. Hence, we exclude the antenv virtual environment directory from the deployment artifact to reduce the payload size. 
      - name: Upload artifact for deployment jobs
        uses: actions/upload-artifact@v4
        with:
          name: python-app
          path: |
            .
            !antenv/

      # 🚫 Opting Out of Oryx Build
      # If you prefer to disable the Oryx build process during deployment, follow these steps:
      # 1. Remove the SCM_DO_BUILD_DURING_DEPLOYMENT app setting from your Azure App Service Environment variables.
      # 2. Refer to sample workflows for alternative deployment strategies: https://github.com/Azure/actions-workflow-samples/tree/master/AppService
      

  deploy:
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: python-app
      
      - name: 'Deploy to Azure Web App'
        uses: azure/webapps-deploy@v3
        id: deploy-to-webapp
        with:
          app-name: 'EcomerceApp'
          slot-name: 'Production'
          package: .
      
      - name: 'Configure Environment Variables'
        uses: azure/appservice-settings@v1
        with:
          app-name: 'EcomerceApp'
          slot-name: 'Production'
          app-settings-json: |
            [
              {
                "name": "DB_TYPE",
                "value": "sqlserver",
                "slotSetting": false
              },
              {
                "name": "DB_USER",
                "value": "sa",
                "slotSetting": false
              },
              {
                "name": "DB_PASSWORD",
                "value": "Pantone123",
                "slotSetting": false
              },
              {
                "name": "DB_HOST",
                "value": "localhost",
                "slotSetting": false
              },
              {
                "name": "DB_NAME",
                "value": "tecnolarUnificado",
                "slotSetting": false
              },
              {
                "name": "DB_DRIVER",
                "value": "ODBC Driver 17 for SQL Server",
                "slotSetting": false
              },
              {
                "name": "SMTP_SERVER",
                "value": "smtp.gmail.com",
                "slotSetting": false
              },
              {
                "name": "SMTP_PORT",
                "value": "587",
                "slotSetting": false
              },
              {
                "name": "USERNAME_EMAIL",
                "value": "fjuansimon@gmail.com",
                "slotSetting": false
              },
              {
                "name": "PASSWORD_EMAIL",
                "value": "kfiq isrr gyjw qtng",
                "slotSetting": false
              },
              {
                "name": "MAIL_FROM",
                "value": "fjuansimon@gmail.com",
                "slotSetting": false
              },
              {
                "name": "ALGORITHM",
                "value": "HS256",
                "slotSetting": false
              },
              {
                "name": "ACCESS_TOKEN_DURATION",
                "value": "360",
                "slotSetting": false
              },
              {
                "name": "SECRET",
                "value": "11018517e34d137b782c1f6af9d9e20ebb8ec6425b5b97689e2715b6cb3187c4",
                "slotSetting": false
              },
              {
                "name": "ENVIRONMENT",
                "value": "production",
                "slotSetting": false
              },
              {
                "name": "BASE_URL",
                "value": "https://ecomerceapp.azurewebsites.net",
                "slotSetting": false
              },
              {
                "name": "FRONTEND_URL",
                "value": "https://ecomerceapp.azurewebsites.net",
                "slotSetting": false
              },
              {
                "name": "MERCADOPAGO_ACCESS_TOKEN",
                "value": "TEST-4638869150198277-110316-202a1385377cfc96c5f43eab7829c5ce-168706559",
                "slotSetting": false
              },
              {
                "name": "MERCADOPAGO_PUBLIC_KEY",
                "value": "TEST-6c596386-b72b-444b-8402-d59bab22540f",
                "slotSetting": false
              },
              {
                "name": "ORIGINS",
                "value": "https://ecomerceapp.azurewebsites.net",
                "slotSetting": false
              }
            ]